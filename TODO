TODO

-add a 'help' argument for the CalcServer

DONE

- add a way to adapt the width of the pile in its toString method (by checking the size of the object's representation when putting them in
- Detach the PileRPL object creation from CalcUI to allow a unique PileRPL to be shared by many users/threads
- Add other operations (mult, div)
-Find a way to print the errors back to the user
